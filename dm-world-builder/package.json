{
  "name": "dm-world-builder",
  "version": "1.0.0",
  "description": "Comprehensive DM World Builder Portal with real-time editing and campaign management",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "start": "cd server && npm start"
  },
  "keywords": ["dnd", "dungeon-master", "world-builder", "campaign-management", "real-time"],
  "author": "DM World Builder Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}